required:
  - first_login_within
  - first_login_within_unit
  - valid_time_period
  - valid_time_period_unit
type: object
properties:
  valid_time_period:
    type: integer
    description: The valid time period after the first login
    format: int32
  valid_time_period_unit:
    $ref: ./XiqDateTimeUnitType.yaml
  first_login_within:
    type: integer
    description: The first time the access key must be used
    format: int32
  first_login_within_unit:
    $ref: ./XiqDateTimeUnitType.yaml
description: >-
  The settings for the valid time period after First Login option or null for
  the other option
