openapi: 3.0.3
info:
  title: ExtremeCloud IQ API
  description: ExtremeCloud IQ RESTful API for external and internal applications.
  termsOfService: 'https://www.extremenetworks.com/company/legal/terms-of-use/'
  contact:
    name: Extreme Networks Support
    url: 'https://www.extremenetworks.com/support'
    email: support@extremenetworks.com
  license:
    name: Apache 2.0
    url: 'https://www.apache.org/licenses/LICENSE-2.0.html'
  version: 22.4.0.0
externalDocs:
  description: API Reference
  url: 'https://extremecloudiq.com/api-docs/api-reference.html'
servers:
  - url: '{base_url}'
    description: ExtremeCloud IQ REST API Server
    variables:
      base_url:
        description: ExtremeCloud IQ Production
        default: 'https://api.extremecloudiq.com'
tags:
  - name: Configuration - Policy
    description: SSID/User Profile/Classification Rule/CCG/Radio Profile/...
paths:
  '/ccgs/{id}':
    get:
      tags:
        - Configuration - Policy
      summary: Get a cloud config group
      description: Get cloud config group info for the specified ID.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_get_cloud_config_group'
      operationId: getCloudConfigGroup
      parameters:
        - name: id
          in: path
          description: The cloud config group ID
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/XiqCloudConfigGroup'
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    put:
      tags:
        - Configuration - Policy
      summary: Update cloud config group information
      description: Update the cloud config group details having the specified ID.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_update_cloud_config_group'
      operationId: updateCloudConfigGroup
      parameters:
        - name: id
          in: path
          description: The cloud config group ID
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        description: Update cloud config group request body
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/XiqUpdateCloudConfigGroupRequest'
        required: true
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/XiqCloudConfigGroup'
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    delete:
      tags:
        - Configuration - Policy
      summary: Delete a cloud config group
      description: Delete a specific cloud config group by ID.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_delete_cloud_config_group'
      operationId: deleteCloudConfigGroup
      parameters:
        - name: id
          in: path
          description: The cloud config group ID
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  /ccgs:
    get:
      tags:
        - Configuration - Policy
      summary: List clould config groups
      description: List a papge of cloud config groups.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_list_cloud_config_groups'
      operationId: listCloudConfigGroups
      parameters:
        - name: page
          in: query
          description: 'Page number, min = 1'
          required: false
          schema:
            minimum: 1
            type: integer
            format: int32
            default: 1
        - name: limit
          in: query
          description: 'Page Size, min = 1, max = 100'
          required: false
          schema:
            maximum: 100
            minimum: 1
            type: integer
            format: int32
            default: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PagedXiqCloudConfigGroup'
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    post:
      tags:
        - Configuration - Policy
      summary: Create new cloud config group
      description: Create a new cloud config group.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_create_cloud_config_group'
      operationId: createCloudConfigGroup
      requestBody:
        description: Create new cloud config group request body
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/XiqCreateCloudConfigGroupRequest'
        required: true
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/XiqCloudConfigGroup'
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  /classification-rules:
    get:
      tags:
        - Configuration - Policy
      summary: List classification rules
      description: List a page of classification rules.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_list_the_classification_rules'
      operationId: listClassificationRules
      parameters:
        - name: page
          in: query
          description: 'Page number, min = 1'
          required: false
          schema:
            minimum: 1
            type: integer
            format: int32
            default: 1
        - name: limit
          in: query
          description: 'Page Size, min = 1, max = 100'
          required: false
          schema:
            maximum: 100
            minimum: 1
            type: integer
            format: int32
            default: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PagedXiqClassificationRule'
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    post:
      tags:
        - Configuration - Policy
      summary: Create classification rule
      description: Create a new classification rule.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_create_classification_rule_configuration'
      operationId: createClassificationRule
      requestBody:
        description: The payload to create a new classification rule
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/XiqCreateClassificationRuleRequest'
        required: true
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/XiqClassificationRule'
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  '/classification-rules/{id}':
    get:
      tags:
        - Configuration - Policy
      summary: Get a classification rule by ID
      description: Get a specific classification rule.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_get_classification_rule_by_id'
      operationId: getClassificationRule
      parameters:
        - name: id
          in: path
          description: The classification Rule ID
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/XiqClassificationRule'
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    put:
      tags:
        - Configuration - Policy
      summary: Update classification rule
      description: Update the exist classification rule.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_update_classification_rule_configuration'
      operationId: updateClassificationRule
      parameters:
        - name: id
          in: path
          description: The classification rule ID
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        description: The payload to update exist classification rule
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/XiqUpdateClassificationRuleRequest'
        required: true
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/XiqClassificationRule'
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    delete:
      tags:
        - Configuration - Policy
      summary: Delete classification rule by ID
      description: Delete an existing classification rule by ID.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_delete_classification_rule_configuration'
      operationId: deleteClassificationRule
      parameters:
        - name: id
          in: path
          description: The classification rule ID
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  /l3-address-profiles:
    get:
      tags:
        - Configuration - Policy
      summary: List L3 address profiles
      description: List all L3 Address Profiles.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_list_the_l3_address_profile'
      operationId: listL3AddressProfiles
      parameters:
        - name: addressType
          in: query
          description: The address type
          required: true
          schema:
            type: string
            enum:
              - L3_ADDRESS_TYPE_UNSPECIFIED
              - L3_ADDRESS_TYPE_IP_ADDRESS
              - L3_ADDRESS_TYPE_IP_SUBNET
              - L3_ADDRESS_TYPE_IP_RANGE
              - UNRECOGNIZED
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/XiqL3AddressProfile'
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  "/radio-profiles":
    get:
      tags:
        - Configuration - Policy
      summary: List radio profiles
      description: List a page of radio profiles.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_list_the_radio_profiles
      operationId: listRadioProfiles
      parameters:
        - name: page
          in: query
          description: Page number, min = 1
          required: false
          schema:
            minimum: 1
            type: integer
            format: int32
            default: 1
        - name: limit
          in: query
          description: Page Size, min = 1, max = 100
          required: false
          schema:
            maximum: 100
            minimum: 1
            type: integer
            format: int32
            default: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PagedXiqRadioProfile"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    post:
      tags:
        - Configuration - Policy
      summary: Create a radio profile
      description: Create a new radio profile.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_create_radio_profile_configuration
      operationId: createRadioProfile
      requestBody:
        description: The request body to create new user profile.
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/XiqCreateRadioProfileRequest"
        required: true
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/XiqRadioProfile"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  "/radio-profiles/mac-ouis":
    get:
      tags:
        - Configuration - Policy
      summary: List MAC OUI profiles
      description: List a page of MAC OUI profiles.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_list_mac_oui_profiles
      operationId: listRpMacOuiProfiles
      parameters:
        - name: page
          in: query
          description: Page number, min = 1
          required: false
          schema:
            minimum: 1
            type: integer
            format: int32
            default: 1
        - name: limit
          in: query
          description: Page Size, min = 1, max = 100
          required: false
          schema:
            maximum: 100
            minimum: 1
            type: integer
            format: int32
            default: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/PagedXiqRpMacOuiProfile"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    post:
      tags:
        - Configuration - Policy
      summary: Create a MAC OUI profile
      description: Create a new MAC OUI profile for radio usage optimization.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_create_mac_oui_profile_configuration
      operationId: createMacOuiProfile
      requestBody:
        description: The request body to create new user profile.
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/XiqCreateRpMacOuiProfileRequest"
        required: true
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/XiqRpMacOuiProfile"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  "/radio-profiles/{id}":
    get:
      tags:
        - Configuration - Policy
      summary: Get radio profile by ID
      description: Get radio profile details for the specified ID.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_get_radio_profile_by_id
      operationId: getRadioProfile
      parameters:
        - name: id
          in: path
          description: The radio profile ID
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/XiqRadioProfile"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    put:
      tags:
        - Configuration - Policy
      summary: Update radio profile by ID
      description: Update the existing radio profile by the profile ID.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_update_radio_profile_configuration
      operationId: updateRadioProfile
      parameters:
        - name: id
          in: path
          description: The radio profile ID.
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        description: The payload of the update radio profile request.
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/XiqUpdateRadioProfileRequest"
        required: true
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/XiqRadioProfile"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    delete:
      tags:
        - Configuration - Policy
      summary: Delete radio profile by ID
      description: Delete the existing radio profile by the profile ID.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_delete_radio_profile_configuration
      operationId: deleteRadioProfile
      parameters:
        - name: id
          in: path
          description: The radio profile ID
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  "/radio-profiles/wmm-qos/{id}":
    get:
      tags:
        - Configuration - Policy
      summary: Get Wmm QoS settings
      description: Get the Wi-Fi Multimedia (WMM) QoS settings belonging to a radio
        profile.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_get_wmm_qos_settings_by_id
      operationId: getRpWmmQosSettings
      parameters:
        - name: id
          in: path
          description: The radio QoS settings ID
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/XiqRpWmmQosSettings"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    put:
      tags:
        - Configuration - Policy
      summary: Update Wmm QoS settings
      description: Update the Wi-Fi Multimedia (WMM) QoS settings belonging to a radio
        profile.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_update_wmm_qos_settings_by_id
      operationId: updateRpWmmQosSettings
      parameters:
        - name: id
          in: path
          description: The Wmm QoS settings ID.
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        description: The payload of the update Wmm QoS settings request.
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/XiqUpdateRpWmmQosSettingsRequest"
        required: true
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/XiqRpWmmQosSettings"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  "/radio-profiles/sensor-scan/{id}":
    get:
      tags:
        - Configuration - Policy
      summary: Get sensor scan settings
      description: Get the sensor scan settings belonging to a radio profile.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_get_sensor_scan_settings_by_id
      operationId: getRpSensorScanSettings
      parameters:
        - name: id
          in: path
          description: The sensor scan settings ID
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/XiqRpSensorScanSettings"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    put:
      tags:
        - Configuration - Policy
      summary: Update sensor scan settings
      description: Update the sensor scan settings belonging to a radio profile.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_update_sensor_scan_settings_by_id
      operationId: updateRpSensorScanSettings
      parameters:
        - name: id
          in: path
          description: The sensor scan settings ID.
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        description: The payload of the update sensor scan settings request.
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/XiqUpdateRpSensorScanSettingsRequest"
        required: true
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/XiqRpSensorScanSettings"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  "/radio-profiles/radio-usage-opt/{id}":
    get:
      tags:
        - Configuration - Policy
      summary: Get radio usage optimization settings
      description: Get the radio usage optimization settings belonging to a radio
        profile.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_get_radio_usage_optimization_by_id
      operationId: getRpRadioUsageOptimization
      parameters:
        - name: id
          in: path
          description: The radio usage optimization settings ID
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/XiqRpRadioUsageOptimization"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    put:
      tags:
        - Configuration - Policy
      summary: Update radio usage optimization settings
      description: Update the radio usage optimization settings belonging to a radio
        profile.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_update_radio_usage_optimization_by_id
      operationId: updateRpRadioUsageOptimization
      parameters:
        - name: id
          in: path
          description: The radio usage optimization settings ID.
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        description: The payload of the update radio usage optimization settings request.
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/XiqUpdateRpRadioUsageOptimizationRequest"
        required: true
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/XiqRpRadioUsageOptimization"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  "/radio-profiles/neighborhood-analysis/{id}":
    get:
      tags:
        - Configuration - Policy
      summary: Get neighborhood analysis settings
      description: Get the neighborhood analysis settings belonging to a radio profile.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_get_radio_profile_neighborhood_analysis_by_id
      operationId: getNeighborhoodAnalysis
      parameters:
        - name: id
          in: path
          description: The neighborhood analysis settings ID
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/XiqRpNeighborhoodAnalysis"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    put:
      tags:
        - Configuration - Policy
      summary: Update neighborhood analysis settings
      description: Update the neighborhood analysis settings belonging to a radio
        profile.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_update_radio_profile_neighborhood_analysis_by_id
      operationId: updateNeighborhoodAnalysis
      parameters:
        - name: id
          in: path
          description: The neighborhood analysis settings ID.
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        description: The payload of the update neighborhood analysis settings request.
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/XiqUpdateRpNeighborhoodAnalysisRequest"
        required: true
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/XiqRpNeighborhoodAnalysis"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  "/radio-profiles/miscellaneous/{id}":
    get:
      tags:
        - Configuration - Policy
      summary: Get radio miscellaneous settings
      description: Get the radio miscellaneous settings belonging to a radio profile.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_get_radio_miscellaneous_settings_by_id
      operationId: getRpMiscellaneousSettings
      parameters:
        - name: id
          in: path
          description: The radio miscellaneous settings ID
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/XiqRpMiscellaneousSettings"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    put:
      tags:
        - Configuration - Policy
      summary: Update radio miscellaneous settings
      description: Update the radio miscellaneous settings belonging to a radio profile.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_update_radio_miscellaneous_settings_by_id
      operationId: updateRpMiscellaneousSettings
      parameters:
        - name: id
          in: path
          description: The radio miscellaneous settings ID.
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        description: The payload of the update radio miscellaneous settings request.
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/XiqUpdateRpMiscellaneousSettingsRequest"
        required: true
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/XiqRpMiscellaneousSettings"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  "/radio-profiles/mac-ouis/{id}":
    get:
      tags:
        - Configuration - Policy
      summary: Get MAC OUI profile
      description: Get the MAC OUI profile belonging the radio optimization settings.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_get_mac_oui_profile_by_id
      operationId: getRpMacOuiProfile
      parameters:
        - name: id
          in: path
          description: The MAC OUI profile ID
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/XiqRpMacOuiProfile"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    put:
      tags:
        - Configuration - Policy
      summary: Update MAC OUI profile
      description: Update the existing MAC OUI profile for radio usage optimization.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_update_mac_oui_profile_by_id
      operationId: updateRpMacOuiProfile
      parameters:
        - name: id
          in: path
          description: The MAC OUI profile ID.
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        description: The payload of the update MAC OUI profile request.
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/XiqUpdateRpMacOuiProfileRequest"
        required: true
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/XiqRpMacOuiProfile"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    delete:
      tags:
        - Configuration - Policy
      summary: Delete MAC OUI profile
      description: Delete the existing MAC OUI profile for radio usage optimization.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_delete_mac_oui_profile_by_id
      operationId: deleteRpMacOuiProfile
      parameters:
        - name: id
          in: path
          description: The MAC OUI profile ID
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  "/radio-profiles/channel-selection/{id}":
    get:
      tags:
        - Configuration - Policy
      summary: Get channel selection settings
      description: Get the channel selection settings belonging to a radio profile.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_get_radio_profile_channel_selection_by_id
      operationId: getRpChannelSelection
      parameters:
        - name: id
          in: path
          description: The channel selection settings ID
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/XiqRpChannelSelection"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    put:
      tags:
        - Configuration - Policy
      summary: Update channel selection settings
      description: Update the channel selection settings belonging to a radio profile.
      externalDocs:
        description: API Reference
        url: http://localhost:8081/api-docs/api-reference.html#_update_radio_profile_channel_selection_by_id
      operationId: updateRpChannelSelection
      parameters:
        - name: id
          in: path
          description: The channel selection settings ID.
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        description: The payload of the update channel selection settings request.
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/XiqUpdateRpChannelSelectionRequest"
        required: true
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/XiqRpChannelSelection"
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  /ssids:
    get:
      tags:
        - Configuration - Policy
      summary: List SSIDs
      description: List SSIDs with filter and pagination.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_list_ssids'
      operationId: listSsids
      parameters:
        - name: page
          in: query
          description: 'Page number, min = 1'
          required: false
          schema:
            minimum: 1
            type: integer
            format: int32
            default: 1
        - name: limit
          in: query
          description: 'Page Size, min = 1, max = 100'
          required: false
          schema:
            maximum: 100
            minimum: 1
            type: integer
            format: int32
            default: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PagedXiqSsid'
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  '/ssids/{id}/psk/password':
    put:
      tags:
        - Configuration - Policy
      summary: Change the SSID PSK password
      description: Change the SSID PSK password.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_change_the_ssid_psk_password'
      operationId: changePskPassword
      parameters:
        - name: id
          in: path
          description: The SSID ID
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        description: The new SSID PSK password
        content:
          application/json:
            schema:
              type: string
        required: true
      responses:
        '200':
          description: OK
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  '/ssids/{id}/mode/wep':
    put:
      tags:
        - Configuration - Policy
      summary: Change the SSID mode to WEP
      description: Change the SSID mode to WEP.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_set_ssid_mode_to_wep'
      operationId: setSsidModeWep
      parameters:
        - name: id
          in: path
          description: The SSID ID
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        description: The payload to change the SSID mode to WEP
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/XiqSetSsidModeWepRequest'
        required: true
      responses:
        '200':
          description: OK
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  '/ssids/{id}/mode/psk':
    put:
      tags:
        - Configuration - Policy
      summary: Change the SSID mode to PSK
      description: Change the SSID mode to PSK.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_set_ssid_mode_to_psk'
      operationId: setSsidModePsk
      parameters:
        - name: id
          in: path
          description: The SSID ID
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        description: The payload to change the SSID mode to PSK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/XiqSetSsidModePskRequest'
        required: true
      responses:
        '200':
          description: OK
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  '/ssids/{id}/mode/ppsk':
    put:
      tags:
        - Configuration - Policy
      summary: Change the SSID mode to PPSK
      description: Change the SSID mode to PPSK.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_set_ssid_mode_to_ppsk'
      operationId: setSsidModePpsk
      parameters:
        - name: id
          in: path
          description: The SSID ID
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        description: The payload to change the SSID mode to PPSK
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/XiqSetSsidModePpskRequest'
        required: true
      responses:
        '200':
          description: OK
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  '/ssids/{id}/mode/open':
    put:
      tags:
        - Configuration - Policy
      summary: Change the SSID mode to open access
      description: Change the SSID mode to open access.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_set_ssid_mode_to_open_access'
      operationId: setSsidModeOpen
      parameters:
        - name: id
          in: path
          description: The SSID ID
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  '/ssids/{id}/mode/dot1x':
    put:
      tags:
        - Configuration - Policy
      summary: Change the SSID mode to 802.1x
      description: Change the SSID mode to 802.1x.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_set_ssid_mode_to_dot1x'
      operationId: setSsidModeDot1x
      parameters:
        - name: id
          in: path
          description: The SSID ID
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        description: The payload to change the SSID mode to 802.1x
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/XiqSetSsidModeDot1xRequest'
        required: true
      responses:
        '200':
          description: OK
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  /user-profiles:
    get:
      tags:
        - Configuration - Policy
      summary: List user profiles
      description: List a page of user profiles.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_list_the_user_profiles'
      operationId: listUserProfiles
      parameters:
        - name: page
          in: query
          description: 'Page number, min = 1'
          required: false
          schema:
            minimum: 1
            type: integer
            format: int32
            default: 1
        - name: limit
          in: query
          description: 'Page Size, min = 1, max = 100'
          required: false
          schema:
            maximum: 100
            minimum: 1
            type: integer
            format: int32
            default: 10
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PagedXiqUserProfile'
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    post:
      tags:
        - Configuration - Policy
      summary: Create a user profile
      description: Create a new user profile.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_create_user_profile_configuration'
      operationId: createUserProfile
      requestBody:
        description: The request body to create new user profile.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/XiqCreateUserProfileRequest'
        required: true
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/XiqUserProfile'
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
  '/user-profiles/{id}':
    get:
      tags:
        - Configuration - Policy
      summary: Get user profile by ID
      description: Get user profile details for the specified ID.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_get_user_profile_by_id'
      operationId: getUserProfile
      parameters:
        - name: id
          in: path
          description: The user profile ID
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/XiqUserProfile'
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    put:
      tags:
        - Configuration - Policy
      summary: Update user profile
      description: Update an existing user profile.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_update_user_profile_configuration'
      operationId: updateCoUserProfile
      parameters:
        - name: id
          in: path
          description: The user profile ID.
          required: true
          schema:
            type: integer
            format: int64
      requestBody:
        description: The payload of user profile.
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/XiqUpdateUserProfileRequest'
        required: true
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/XiqUserProfile'
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
    delete:
      tags:
        - Configuration - Policy
      summary: Delete an user profile by ID
      description: Delete an existing user profile by ID.
      externalDocs:
        description: API Reference
        url: 'https://extremecloudiq.com/api-docs/api-reference.html#_delete_user_profile_configuration'
      operationId: deleteCoUserProfile
      parameters:
        - name: id
          in: path
          description: The user profile ID
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: OK
        default:
          $ref: 'common/components.yaml#/components/responses/ErrorResponse'
      security:
        - BearerAuth: []
components:
  securitySchemes:
    BearerAuth:
      type: http
      description: JSON Web Token (JWT) based authentication
      scheme: bearer
      bearerFormat: JWT
  schemas:
    XiqUpdateCloudConfigGroupRequest:
      type: object
      properties:
        name:
          type: string
          description: The CCG name
        description:
          type: string
          description: The CCG description
        device_ids:
          type: array
          description: The device ID list.
          items:
            type: integer
            description: The device ID list.
            format: int64
    XiqCloudConfigGroup:
      required:
        - create_time
        - id
        - name
        - predefined
        - update_time
      type: object
      properties:
        id:
          type: integer
          description: The unique identifier
          format: int64
        create_time:
          type: string
          description: The create time
          format: date-time
        update_time:
          type: string
          description: The last update time
          format: date-time
        org_id:
          type: integer
          description: 'The organization identifier, valid when enabling HIQ feature'
          format: int64
        name:
          type: string
          description: The CCG name
        description:
          type: string
          description: The CCG description
        predefined:
          type: boolean
          description: Whether it is predefined
        read_only:
          type: boolean
          description: Whether it is read-only
        zone_name:
          type: string
          description: The zone name.
        zone_id:
          type: integer
          description: The zone ID
          format: int64
        zone_location_id:
          type: integer
          description: The zone location ID
          format: int64
        device_ids:
          type: array
          description: The device IDs selected for this Ccg.
          items:
            type: integer
            description: The device IDs selected for this Ccg.
            format: int64
      description: The CCG
    PagedXiqClassificationRule:
      required:
        - count
        - page
        - total_count
        - total_pages
      type: object
      properties:
        page:
          type: integer
          description: The current page number
          format: int32
        count:
          type: integer
          description: The element count of the current page
          format: int32
        total_pages:
          type: integer
          description: The total page number based on request page size
          format: int32
        total_count:
          type: integer
          description: The total element count
          format: int64
        data:
          type: array
          description: The data in the current page
          items:
            $ref: '#/components/schemas/XiqClassificationRule'
    PagedXiqCloudConfigGroup:
      required:
        - count
        - page
        - total_count
        - total_pages
      type: object
      properties:
        page:
          type: integer
          description: The current page number
          format: int32
        count:
          type: integer
          description: The element count of the current page
          format: int32
        total_pages:
          type: integer
          description: The total page number based on request page size
          format: int32
        total_count:
          type: integer
          description: The total element count
          format: int64
        data:
          type: array
          description: The data in the current page
          items:
            $ref: '#/components/schemas/XiqCloudConfigGroup'
    XiqCreateClassificationRequest:
      required:
        - classification_type
        - classification_type_id
        - match
      type: object
      properties:
        classification_type:
          type: string
          description: Classification type
          enum:
            - CLASSIFICATION_TYPE_UNSPECIFIED
            - CLASSIFICATION_TYPE_LOCATION
            - CLASSIFICATION_TYPE_CLOUD_CONFIG_GROUP
            - CLASSIFICATION_TYPE_IP_ADDRESS
            - CLASSIFICATION_TYPE_IP_SUBNET
            - CLASSIFICATION_TYPE_IP_RANGE
            - UNRECOGNIZED
        match:
          type: boolean
          description: Contains or not contains
        classification_type_id:
          type: integer
          description: 'The ID of location, cloud config group, IP address, IP subnet or IP range.'
          format: int64
      description: The details of rule assignments
    XiqCreateClassificationRuleRequest:
      required:
        - classifications
        - name
      type: object
      properties:
        name:
          type: string
          description: The name of classification rule
        description:
          type: string
          description: The description of classification rule
        classifications:
          type: array
          description: The details of rule assignments
          items:
            $ref: '#/components/schemas/XiqCreateClassificationRequest'
    XiqCreateCloudConfigGroupRequest:
      required:
        - name
      type: object
      properties:
        name:
          type: string
          description: The CCG name
        description:
          type: string
          description: The CCG description
        device_ids:
          type: array
          description: The device ID list.
          items:
            type: integer
            description: The device ID list.
            format: int64
    XiqL3AddressProfile:
      required:
        - address_type
        - create_time
        - id
        - name
        - update_time
      type: object
      properties:
        id:
          type: integer
          description: The unique identifier
          format: int64
        create_time:
          type: string
          description: The create time
          format: date-time
        update_time:
          type: string
          description: The last update time
          format: date-time
        address_type:
          $ref: '#/components/schemas/XiqL3AddressType'
        name:
          type: string
          description: Address profile name
    XiqL3AddressType:
      type: string
      description: Address type
      enum:
        - IP_ADDRESS
        - IP_SUBNET
        - IP_RANGE
    XiqSetSsidModeWepRequest:
      required:
        - encryption_method
        - key_management
      type: object
      properties:
        key_management:
          $ref: '#/components/schemas/XiqSsidWepKeyManagement'
        encryption_method:
          $ref: '#/components/schemas/XiqSsidWepEncryptionMethod'
        authentication_method:
          $ref: '#/components/schemas/XiqSsidWepAuthenticationMethod'
        default_key:
          $ref: '#/components/schemas/XiqSsidWepDefaultKey'
        key_type:
          $ref: '#/components/schemas/XiqSsidKeyType'
        key_value:
          maxLength: 13
          minLength: 5
          type: string
          description: 'The first key value, must be 13 characters long for WEP104, and 5 characters long for WEP40, cannot be null if it is the default key'
          example: abcd123456789
        key_value2:
          maxLength: 13
          minLength: 5
          type: string
          description: 'The second key value, must be 13 characters long for WEP104, and 5 characters long for WEP40, cannot be null if it is the default key'
        key_value3:
          maxLength: 13
          minLength: 5
          type: string
          description: 'The third key value, must be 13 characters long for WEP104, and 5 characters long for WEP40, cannot be null if it is the default key'
        key_value4:
          maxLength: 13
          minLength: 5
          type: string
          description: 'The fourth key value, must be 13 characters long for WEP104, and 5 characters long for WEP40, cannot be null if it is the default key'
        radius_server_group_id:
          type: integer
          description: The RADIUS server group ID if using WEP_8021x as the key management
          format: int64
      description: The request for setting the SSID to be WEP mode.
    XiqRadioProfile:
      required:
        - create_time
        - id
        - update_time
      type: object
      properties:
        id:
          type: integer
          description: The unique identifier
          format: int64
        create_time:
          type: string
          description: The create time
          format: date-time
        update_time:
          type: string
          description: The last update time
          format: date-time
        name:
          type: string
          description: The radio profile name
        predefined:
          type: boolean
          description: Whether the radio profile is predefined or user-customized.
        description:
          type: string
          description: The radio profile description.
        transmission_power:
          type: integer
          description: The transmission power floor from 1 up to 20 dBm or null for
            Auto.
          format: int32
        max_transmit_power:
          type: integer
          description: The maximum transmit power from 10 up to 20 dBm.
          format: int32
        transmission_power_floor:
          type: integer
          description: The transmission power floor from 2 up to 20 dBm.
          format: int32
        transmission_power_max_drop:
          type: integer
          description: The transmission power max drop from 0 up to 18 dB.
          format: int32
        max_clients:
          type: integer
          description: The maximum number of clients from 1 up to 255.
          format: int32
        enable_client_transmission_power:
          type: boolean
          description: Whether or not client transmission power control (802.11h)
            is enabled.
        client_transmission_power:
          type: integer
          description: The client transmission power from 1 up to 20 dBm if it is
            enabled.
          format: int32
        enable_ofdma:
          type: boolean
          description: Whether to enable Orthogonal Frequency Division Multiple Access
            (802.11ax) for multiple-user access by subdividing a channel.
        radio_mode:
          "$ref": "#/components/schemas/XiqRadioMode"
        neighborhood_analysis_id:
          type: integer
          description: The neighborhood analysis Id.
          format: int64
        channel_selection_id:
          type: integer
          description: The channel selection Id.
          format: int64
        radio_usage_optimization_id:
          type: integer
          description: The radio usage optimization Id.
          format: int64
        miscellaneous_settings_id:
          type: integer
          description: The miscellaneous settings Id
          format: int64
        presence_server_settings_id:
          type: integer
          description: The presence server settings Id.
          format: int64
        sensor_scan_settings_id:
          type: integer
          description: The sensor scan settings Id.
          format: int64
      description: The payload of Radio Profile
    PagedXiqRadioProfile:
      required:
        - count
        - page
        - total_count
        - total_pages
      type: object
      properties:
        page:
          type: integer
          description: The current page number
          format: int32
        count:
          type: integer
          description: The element count of the current page
          format: int32
        total_pages:
          type: integer
          description: The total page number based on request page size
          format: int32
        total_count:
          type: integer
          description: The total element count
          format: int64
        data:
          type: array
          description: The data in the current page
          items:
            "$ref": "#/components/schemas/XiqRadioProfile"
    XiqCreateRadioProfileRequest:
      required:
        - name
      type: object
      properties:
        name:
          type: string
          description: The radio profile name
        description:
          type: string
          description: The radio profile description.
        transmission_power:
          maximum: 20
          minimum: 1
          type: integer
          description: The transmission power floor in the range of 1-20 dBm or null
            for Auto.
          format: int32
        max_transmit_power:
          maximum: 20
          minimum: 10
          type: integer
          description: The maximum transmit power in the range of 10-20 dBm.
          format: int32
        transmission_power_floor:
          maximum: 20
          minimum: 2
          type: integer
          description: The transmission power floor in the range of 2-20 dBm.
          format: int32
        transmission_power_max_drop:
          maximum: 18
          minimum: 0
          type: integer
          description: The transmission power max drop in the range of 0-18 dB.
          format: int32
        max_clients:
          maximum: 255
          minimum: 1
          type: integer
          description: The maximum number of clients in the range of 1-255.
          format: int32
        enable_client_transmission_power:
          type: boolean
          description: Whether or not client transmission power control (802.11h)
            is enabled.
        client_transmission_power:
          maximum: 20
          minimum: 1
          type: integer
          description: The client transmission power (in the range of 1-20 dBm) if
            it is enabled.
          format: int32
        radio_mode:
          "$ref": "#/components/schemas/XiqRadioMode"
        enable_ofdma:
          type: boolean
          description: Whether to enable Orthogonal Frequency Division Multiple Access
            (802.11ax) for multiple-user access by subdividing a channel.
    XiqCreateRpMacOuiProfileRequest:
      required:
        - name
        - value
      type: object
      properties:
        name:
          type: string
          description: The product name
        value:
          type: string
          description: The product MAC or OUI
        description:
          type: string
          description: The product description
        mac_type:
          type: string
          description: The json type, eg, "mac-oui-profile"  or "MAC_OUI"
        defender_defined:
          type: boolean
          description: Whether defender is defined
    XiqUpdateRpWmmQosSettingsRequest:
      type: object
      properties:
        arbitration_interframe_space:
          maximum: 15
          minimum: 1
          type: integer
          description: The Arbitration Interframe space, in the range of 1 to 15.
          format: int32
        min_contention_window:
          maximum: 15
          minimum: 1
          type: integer
          description: The Minimum Contention window, in the range of 1 to 15.
          format: int32
        max_contention_window:
          maximum: 15
          minimum: 1
          type: integer
          description: The Maximum Contention window, in the range of 1 to 15.
          format: int32
        transmission_opportunity_limit:
          maximum: 8192
          minimum: 0
          type: integer
          description: The Transmission Opportunity limit, in the range of 0 to 8192.
          format: int32
        enable_no_ack:
          type: boolean
          description: Whether to enable No Acknowledgment
    XiqRpWmmQosSettings:
      required:
        - create_time
        - id
        - update_time
      type: object
      properties:
        id:
          type: integer
          description: The unique identifier
          format: int64
        create_time:
          type: string
          description: The create time
          format: date-time
        update_time:
          type: string
          description: The last update time
          format: date-time
        access_category:
          type: string
          description: The media categories, including "VOICE", "VIDEO", "BEST_EFFORT",
            and "BACKGROUND"
        arbitration_interframe_space:
          type: integer
          description: The Arbitration Interframe space from 1 up to 15.
          format: int32
        min_contention_window:
          type: integer
          description: The Minimum Contention window from 1 up to 15.
          format: int32
        max_contention_window:
          type: integer
          description: The Maximum Contention window from 1 up to 15.
          format: int32
        transmission_opportunity_limit:
          type: integer
          description: The Transmission Opportunity limit from 0 up to 8192.
          format: int32
        enable_no_ack:
          type: boolean
          description: Whether to enable No Acknowledgment
      description: The payload of config for the radio profile
    XiqUpdateRpSensorScanSettingsRequest:
      type: object
      properties:
        enable_scan_all_channels:
          type: boolean
          description: Whether to enable scanning all channels
        dwell_time:
          type: string
          description: The dwell time in the range from 250 to 30000 ms
        scan_channels:
          type: string
          description: The comma separated list of scan channels
    XiqRpSensorScanSettings:
      required:
        - create_time
        - id
        - update_time
      type: object
      properties:
        id:
          type: integer
          description: The unique identifier
          format: int64
        create_time:
          type: string
          description: The create time
          format: date-time
        update_time:
          type: string
          description: The last update time
          format: date-time
        enable_scan_all_channels:
          type: boolean
          description: Whether to enable scanning all channels
        dwell_time:
          type: string
          description: The dwell time in ms
        scan_channels:
          type: string
          description: The comma separated list of scan channels
      description: The payload of config for the radio profile
    XiqUpdateRpRadioUsageOptimizationRequest:
      type: object
      properties:
        preamble:
          type: string
          description: The preamble data length -- "AUTO" or "LONG"
        beacon_period:
          maximum: 3500
          minimum: 40
          type: integer
          description: The amount of time between beacons in the range from 40 to
            3500.
          format: int32
        enable_frame_burst:
          type: boolean
          description: Whether to enable Frame Burst
        enable_smart_antenna:
          type: boolean
          description: Whether to enable Smart Antenna (Enabling this option will
            disable (Null) for MU-MIMO)
        enable_backhaul_failover:
          type: boolean
          description: Whether to enable backhaul failover. Backhaul failover settings
            determine the thresholds at which the device switches from a wired to
            a wireless backhaul link, and the thresholds at which the device switches
            back
        wireless_backhaul_switch_trigger_time:
          maximum: 5
          minimum: 1
          type: integer
          description: Switch to Wireless Backhaul after the number of seconds (1
            to 5) after the wired link fails
          format: int32
        wired_backhaul_revert_hold_time:
          maximum: 300
          minimum: 1
          type: integer
          description: Revert Back to Wired Backhaul after the number of seconds (1
            to 300) after the wired link is established
          format: int32
        enable_band_steering:
          type: boolean
          description: Whether to enable band steering. Enabling steers clients from
            2.4 GHz to 5.0 GHz radio band
        band_steering_mode:
          type: string
          description: The band steering mode -- "BALANCE", "URGE_5G", or "ENFORCE_5G"
        ratio_for5g_clients:
          maximum: 100
          minimum: 1
          type: integer
          description: The allowed percentage distribution of 2.4 and 5.0 GHz clients
            for "BALANCE" steering mode.
          format: int32
        ignore_initial_client_connection_number:
          maximum: 100
          minimum: 1
          type: integer
          description: The number of connection attempts from 2.4 GHz clients to ignore
            before responding for URGE_5G steering mode.
          format: int32
        enable_client_load_balancing:
          type: boolean
          description: Whether to enable client load balancing.  Enabling load-balances
            clients across neighboring Extreme Networks within the same hive. Set
            WiFi0 and WiFi1 radios to the same load balancing mode when it is based
            on the number of associated stations.
        load_balancing_mode:
          type: string
          description: The client load balancing mode -- "AIRTIME_BASED" or "CLIENT_NUMBER"
        crc_error_rate_per_device:
          maximum: 99
          minimum: 1
          type: integer
          description: The CRC Error rate threshold value for "AIRTIME_BASED" load
            balancing. Ignore probe and association requests per device when CRC Error
            rate exceeds the threshold.
          format: int32
        rf_interference_per_device:
          maximum: 99
          minimum: 1
          type: integer
          description: The RF Interference threshold value for "AIRTIME_BASED" load
            balancing. Ignore probe and association requests per device when RF Interference
            exceeds the threshold.
          format: int32
        average_airtime_per_client:
          maximum: 5
          minimum: 1
          type: integer
          description: The Average Airtime Per Client threshold value for "AIRTIME_BASED"
            load balancing. Ignore probe and association requests per device when
            Average Airtime Per Client exceeds the threshold.
          format: int32
        anchor_period:
          maximum: 600
          minimum: 10
          type: integer
          description: The Anchor Period value for both "AIRTIME_BASED" and "CLIENT_NUMBER"
            load balancing. Ignore probe and association requests from clients associated
            with other Extreme Networks devices until Anchor Period Eelapses in the
            range of 10 to 600 seconds
          format: int32
        neighbor_query_interval:
          maximum: 600
          minimum: 1
          type: integer
          description: In both client load balancing modes, query neighbors about
            client load every in the range of 1 to 600 seconds
          format: int32
        enable_weak_signal_probe_request_suppression:
          type: boolean
          description: Whether to enable Weak Signal Probe Request Suppression. Weak
            Signal Probe Request Suppression allows the configuration of signal-to-noise
            threshold beyond which the device does not respond to client probes.
        weak_snr_threshold:
          maximum: 100
          minimum: 1
          type: integer
          description: The signal to noise threshold in the range of 1 to 100 for
            Weak Signal Probe Request Suppression.
          format: int32
        enable_safety_net:
          type: boolean
          description: Whether to enable Safety Net. When a device is overloaded or
            is probed by clients with a low signal-to-noise ratio,  Safety Net allows
            the device to respond to association requests  after a certain time period
            lapses.
        safety_net_period:
          maximum: 300
          minimum: 5
          type: integer
          description: The Safety Net Time Period in the range of 5 to 300 seconds.
          format: int32
        enable_high_density:
          type: boolean
          description: Whether to enable High Density Configuration. Enabling optimizes
            performance in high density environments
        management_frame_basic_data_rate:
          type: string
          description: The data rates to support in high density environment -- "HIGH"
            or "LOW"
        enable_suppress_successive_probe_request:
          type: boolean
          description: Whether to Reduce Response to Probe Requests. Enabling suppresses
            successive requests within the same beacon interval
        probe_response_reduction_option:
          type: string
          description: The suppress response to broadcast probes options --  "ONLY_ONE_SSID_RESPOND_AT_A_TIME"
            (allowed for only one SSID to respond at a time), "REDUCE_CERTAIN_CLIENTS_RESPONSE"
            (reducing responses to certain client devices).
        suppression_limit:
          maximum: 10
          minimum: 1
          type: integer
          description: The Number of Connection Attempts in the range of 1 to 10
          format: int32
        enable_radio_balance:
          type: boolean
          description: Whether to enable Radio Load Balancing. Enabling distributes
            wireless clients that support 5 GHz band evenly across the two radios
            in Dual-5G mode when an SSID is available on both radios
        mac_oui_ids:
          type: array
          description: The MacOui Profile IDs for the "REDUCE_CERTAIN_CLIENTS_RESPONSE"
            probe response reduction option
          items:
            type: integer
            description: The MacOui Profile IDs for the "REDUCE_CERTAIN_CLIENTS_RESPONSE"
              probe response reduction option
            format: int64
        enable_ampdu:
          type: boolean
          description: Enable Aggregate MAC Protocol Data Units to combine data frames
            into larger frames before transmission.
        enable_mu_mimo:
          type: boolean
          description: Whether to enable Multiple-Input Multiple-Output (802.11ac
            & 802.11ax) for multiple-user access by using different spatial streams.
        enable_ofdma_down_link:
          type: boolean
          description: Whether to enable OFDMA for AP downlink communication.
        enable_ofdma_up_link:
          type: boolean
          description: Whether to enable OFDMA for AP uplink communication.
        bss_coloring:
          maximum: 63
          minimum: 1
          type: integer
          description: The numerical identifier of the basic service sets (802.11ax
            ) to identify overlapping basic service sets (OBSSs).
          format: int32
        enable_target_weak_time:
          type: boolean
          description: Whether to enable Target Weak Time.
    XiqRpMacOuiProfile:
      required:
        - create_time
        - id
        - update_time
      type: object
      properties:
        id:
          type: integer
          description: The unique identifier
          format: int64
        create_time:
          type: string
          description: The create time
          format: date-time
        update_time:
          type: string
          description: The last update time
          format: date-time
        name:
          type: string
          description: The product model
        description:
          type: string
          description: The product description
        predefined:
          type: boolean
          description: Whether the MAC Oui is predefined.
        value:
          type: string
          description: The MAC octets
        mac_type:
          type: string
          description: The type or "MAC_OUI"
        defender_defined:
          type: boolean
          description: Whether defender is defined
        extreme_iot_defined:
          type: boolean
          description: Whether is the Extreme Iot Defined
      description: The payload of config for the radio profile
    PagedXiqRpMacOuiProfile:
      required:
        - count
        - page
        - total_count
        - total_pages
      type: object
      properties:
        page:
          type: integer
          description: The current page number
          format: int32
        count:
          type: integer
          description: The element count of the current page
          format: int32
        total_pages:
          type: integer
          description: The total page number based on request page size
          format: int32
        total_count:
          type: integer
          description: The total element count
          format: int64
        data:
          type: array
          description: The data in the current page
          items:
            "$ref": "#/components/schemas/XiqRpMacOuiProfile"
    XiqRpRadioUsageOptimization:
      required:
        - create_time
        - id
        - update_time
      type: object
      properties:
        id:
          type: integer
          description: The unique identifier
          format: int64
        create_time:
          type: string
          description: The create time
          format: date-time
        update_time:
          type: string
          description: The last update time
          format: date-time
        preamble:
          type: string
          description: The preamble data length -- "AUTO" or "LONG"
        beacon_period:
          type: integer
          description: The amount of time between beacons from 40 up to 3500.
          format: int32
        enable_frame_burst:
          type: boolean
          description: Whether to enable Frame Burst
        enable_smart_antenna:
          type: boolean
          description: Whether to enable Smart Antenna (Enabling this option will
            disable (Null) for MU-MIMO)
        enable_backhaul_failover:
          type: boolean
          description: Whether to enable backhaul failover. Backhaul failover settings
            determine the thresholds at which the device switches from a wired to
            a wireless backhaul link, and the thresholds at which the device switches
            back
        wireless_backhaul_switch_trigger_time:
          type: integer
          description: Switch to Wireless Backhaul after 1 up to 5 seconds after the
            wired link fails
          format: int32
        wired_backhaul_revert_hold_time:
          type: integer
          description: Revert Back to Wired Backhaul after 1 up to 300 seconds after
            the wired link is established
          format: int32
        enable_band_steering:
          type: boolean
          description: Whether to enable band steering. Enabling steers clients from
            2.4 GHz to 5.0 GHz radio band
        band_steering_mode:
          type: string
          description: The band steering mode -- "BALANCE", "URGE_5G", or "ENFORCE_5G"
        ignore_initial_client_connection_number:
          type: integer
          description: The number of connection attempts from 1 to 100 for 2.4 GHz
            clients to ignore before responding for URGE_5G steering mode.
          format: int32
        enable_client_load_balancing:
          type: boolean
          description: Whether to enable client load balancing.  Enabling load-balances
            clients across neighboring Extreme Networks within the same hive. Set
            WiFi0 and WiFi1 radios to the same load balancing mode when it is based
            on the number of associated stations.
        load_balancing_mode:
          type: string
          description: The client load balancing mode -- "AIRTIME_BASED" or "CLIENT_NUMBER"
        crc_error_rate_per_device:
          type: integer
          description: The CRC Error rate threshold from 1 up to 99 for "AIRTIME_BASED"
            load balancing. Ignore probe and association requests per device when
            CRC Error rate exceeds the threshold.
          format: int32
        rf_interference_per_device:
          type: integer
          description: The RF Interference threshold from 1 up to 99 for "AIRTIME_BASED"
            load balancing. Ignore probe and association requests per device when
            RF Interference exceeds the threshold.
          format: int32
        average_airtime_per_client:
          type: integer
          description: The Average Airtime Per Client threshold from 1 up to 5 for
            "AIRTIME_BASED" load balancing. Ignore probe and association requests
            per device when Average Airtime Per Client exceeds the threshold.
          format: int32
        anchor_period:
          type: integer
          description: The Anchor Period from 10 up to 600 for both "AIRTIME_BASED"
            and "CLIENT_NUMBER" load balancing. Ignore probe and association requests
            from clients associated with other Extreme Networks devices until Anchor
            Period Eelapses in the range of 10 to 600 seconds
          format: int32
        neighbor_query_interval:
          type: integer
          description: In both client load balancing modes, query neighbors about
            client load every 1 up to 600 seconds
          format: int32
        enable_weak_signal_probe_request_suppression:
          type: boolean
          description: Whether to enable Weak Signal Probe Request Suppression. Weak
            Signal Probe Request Suppression allows the configuration of signal-to-noise
            threshold beyond which the device does not respond to client probes.
        weak_snr_threshold:
          type: integer
          description: The signal to noise threshold from 1 up to 100 for Weak Signal
            Probe Request Suppression.
          format: int32
        enable_safety_net:
          type: boolean
          description: Whether to enable Safety Net. When a device is overloaded or
            is probed by clients with a low signal-to-noise ratio,  Safety Net allows
            the device to respond to association requests  after a certain time period
            lapses.
        safety_net_period:
          type: integer
          description: The Safety Net Time Period from 5 up to 300 seconds.
          format: int32
        enable_high_density:
          type: boolean
          description: Whether to enable High Density Configuration. Enabling optimizes
            performance in high density environments
        management_frame_basic_data_rate:
          type: string
          description: The data rates to support in high density environment -- "HIGH"
            or "LOW"
        enable_suppress_successive_probe_request:
          type: boolean
          description: Whether to Reduce Response to Probe Requests. Enabling suppresses
            successive requests within the same beacon interval
        probe_response_reduction_option:
          type: string
          description: The suppress response to broadcast probes options --  "ONLY_ONE_SSID_RESPOND_AT_A_TIME"
            (allowed for only one SSID to respond at a time), "REDUCE_CERTAIN_CLIENTS_RESPONSE"
            (reducing responses to certain client devices).
        suppression_limit:
          type: integer
          description: The Number of Connection Attempts from 1 up to 10
          format: int32
        enable_radio_balance:
          type: boolean
          description: Whether to enable Radio Load Balancing. Enabling distributes
            wireless clients that support 5 GHz band evenly across the two radios
            in Dual-5G mode when an SSID is available on both radios
        enable_ampdu:
          type: boolean
          description: Enable Aggregate MAC Protocol Data Units to combine data frames
            into larger frames before transmission.
        enable_mu_mimo:
          type: boolean
          description: Whether to enable Multiple-Input Multiple-Output (802.11ac
            & 802.11ax) for multiple-user access by using different spatial streams.
        enable_ofdma_down_link:
          type: boolean
          description: Whether to enable OFDMA for AP downlink communication.
        enable_ofdma_up_link:
          type: boolean
          description: Whether to enable OFDMA for AP uplink communication.
        bss_coloring:
          type: integer
          description: Whether to enable BSS Coloring (802.11ax ) to identify overlapping
            basic service sets (OBSSs).
          format: int32
        enable_target_weak_time:
          type: boolean
          description: Whether to enable Target Weak Time.
        mac_ouis:
          type: array
          description: The device vendor identifiers for the "REDUCE_CERTAIN_CLIENTS_RESPONSE"
            for the probe response reduction option
          items:
            "$ref": "#/components/schemas/XiqRpMacOuiProfile"
        ratio_for_5g_clients:
          type: integer
          description: The percentage distribution from 1 up to 100 for 2.4 and 5.0
            GHz clients for "BALANCE" steering mode.
          format: int32
      description: The payload of config for the radio profile
    XiqUpdateRpNeighborhoodAnalysisRequest:
      type: object
      properties:
        enable_background_scan:
          type: boolean
          description: Whether background scan of neighboring devices is enabled
        background_scan_interval:
          maximum: 1440
          minimum: 1
          type: integer
          description: The background scan interval in the range of 1 to 1440 minutes
          format: int32
        enable_skip_scan_when_clients_connected:
          type: boolean
          description: Whether to skip background scan when devices have client connections
        enable_skip_scan_when_clients_in_power_save_mode:
          type: boolean
          description: Whether to skip background scan when connected devices are
            in power save mode
        enable_skip_scan_when_process_voice_traffic:
          type: boolean
          description: Whether to skip background scan when devices have network traffic
            with voice priority
    XiqRpNeighborhoodAnalysis:
      required:
        - create_time
        - id
        - update_time
      type: object
      properties:
        id:
          type: integer
          description: The unique identifier
          format: int64
        create_time:
          type: string
          description: The create time
          format: date-time
        update_time:
          type: string
          description: The last update time
          format: date-time
        enable_background_scan:
          type: boolean
          description: Whether to enable background scanning of neighboring devices
        background_scan_interval:
          type: integer
          description: The background scan interval from 1 up to 1440 minutes
          format: int32
        enable_skip_scan_when_clients_connected:
          type: boolean
          description: Whether to enable skipping of background scan when devices
            have client connections
        enable_skip_scan_when_clients_in_power_save_mode:
          type: boolean
          description: Whether to skipping of background scan when connected devices
            are in power save mode
        enable_skip_scan_when_process_voice_traffic:
          type: boolean
          description: Whether to enable skipping of background scan when devices
            have network traffic with voice priority
      description: The payload of neighborhood analysis config for the radio profile
    XiqUpdateRpMiscellaneousSettingsRequest:
      type: object
      properties:
        sla_throughput_level:
          type: string
          description: The Client SLA options -- "NORMAL_DENSITY", "HIGH_DENSITY"
            (performance-oriented), or "LOW_DENSITY" (coverage-oriented)
        radio_range:
          maximum: 10000
          minimum: 300
          type: integer
          description: The Outdoor Deployment for signal distance from 300 to 10000
            meters
          format: int32
    XiqRpMiscellaneousSettings:
      required:
        - create_time
        - id
        - update_time
      type: object
      properties:
        id:
          type: integer
          description: The unique identifier
          format: int64
        create_time:
          type: string
          description: The create time
          format: date-time
        update_time:
          type: string
          description: The last update time
          format: date-time
        sla_throughput_level:
          type: string
          description: The Client SLA options -- "NORMAL_DENSITY", "HIGH_DENSITY"
            (performance-oriented), or "LOW_DENSITY" (coverage-oriented)
        radio_range:
          type: integer
          description: The Outdoor Deployment for signal distance from 300 to 10000
            meters
          format: int32
        wmm_qos_settings:
          type: array
          description: The WMM QoS settings for various media types
          items:
            "$ref": "#/components/schemas/XiqRpWmmQosSettings"
      description: The payload of config for the radio profile
    XiqUpdateRpMacOuiProfileRequest:
      type: object
      properties:
        name:
          type: string
          description: The product name
        value:
          type: string
          description: The product MAC or OUI
        description:
          type: string
          description: The product description
        mac_type:
          type: string
          description: The json type, eg, "mac-oui-profile"  or "MAC_OUI"
        defender_defined:
          type: boolean
          description: Whether defender is defined
    XiqUpdateRpChannelSelectionRequest:
      type: object
      properties:
        enable_dynamic_channel_switching:
          type: boolean
          description: Whether to dynamically select and switch channels based on
            the defined criteria.
        channel_width:
          type: string
          description: The channel frequency range
        enable_dynamic_frequency_selection:
          type: boolean
          description: Whether dynamic frequency selection is enabled (a/n, a, ac
            mode)
        enable_static_channel:
          type: boolean
          description: Whether static channel is enabled (manual channel selection
            return)
        enable_zero_wait_dfs:
          type: boolean
          description: Whether ZeroWait DFS is enabled
        enable_use_last_selection:
          type: boolean
          description: Whether to use the last known power and channel during the
            AP boot up process
        exclude_channels:
          type: string
          description: The comma-separated list of excluded channels not on the selected
            channel width.
        exclude_channels_width:
          type: string
          description: The comma-separated list of excluded channels on the selected
            channel width.
        channel:
          maximum: 165
          minimum: 1
          type: integer
          description: The number of channel selections or AUTO for default selection.
          format: int32
        enable_limit_channel_selection:
          type: boolean
          description: Whether to allow for limiting the channel selection to non-overlapping
            channels. (b/g,g/n/, axes modes)
        channel_region:
          type: string
          description: The channel region -- "USA", "Canada", "Europe", or "World"
        channel_model:
          maximum: 4
          minimum: 3
          type: integer
          description: The number of channel models to limit.
          format: int32
        channels:
          type: string
          description: The comma separated list of channels allowed channel switching
        enable_channel_auto_selection:
          type: boolean
          description: Whether to enable automatic channel switching during specified
            time interval.
        channel_selection_start_time:
          type: string
          description: The start time for channel switching in 24-hr time format of
            hh:mm
        channel_selection_end_time:
          type: string
          description: The end time for channel switching in 24-hr time format of
            hh:mm
        enable_avoid_switch_channel_if_clients_connected:
          type: boolean
          description: Whether to avoid channel switching if there are already max
            connected clients
        channel_selection_max_clients:
          maximum: 100
          minimum: 0
          type: integer
          description: The maximum number of connected clients to avoid switching
          format: int32
        enable_switch_channel_if_exceed_threshold:
          type: boolean
          description: Whether to enable channel switching when RF interference exceeds
            the threshold
        rf_interference_threshold:
          maximum: 80
          minimum: 10
          type: integer
          description: The RF interference threshold for channel switching.
          format: int32
        crc_error_threshold:
          maximum: 80
          minimum: 10
          type: integer
          description: The CRC error threshold for channel switching.
          format: int32
    XiqRpChannelSelection:
      required:
        - create_time
        - id
        - update_time
      type: object
      properties:
        id:
          type: integer
          description: The unique identifier
          format: int64
        create_time:
          type: string
          description: The create time
          format: date-time
        update_time:
          type: string
          description: The last update time
          format: date-time
        enable_dynamic_channel_switching:
          type: boolean
          description: Whether to dynamically select and switch channels based on
            the defined criteria.
        channel_width:
          type: string
          description: The channel frequency range
        enable_dynamic_frequency_selection:
          type: boolean
          description: Whether dynamic frequency selection is enabled (a/n, a, ac
            mode)
        enable_static_channel:
          type: boolean
          description: Whether static channel is enabled (manual channel selection
            return)
        enable_zero_wait_dfs:
          type: boolean
          description: Whether ZeroWait DFS is enabled
        enable_use_last_selection:
          type: boolean
          description: Whether to use the last known power and channel during the
            AP boot up process
        exclude_channels:
          type: string
          description: The comma-separated list of excluded channels not on the selected
            channel width.
        exclude_channels_width:
          type: string
          description: The comma-separated list of excluded channels on the selected
            channel width.
        channel:
          type: integer
          description: The number of channel selections from 1 up to 165 or AUTO for
            default selection.
          format: int32
        enable_limit_channel_selection:
          type: boolean
          description: Whether to allow for limiting the channel selection to non-overlapping
            channels. (b/g,g/n/, axes modes)
        channel_region:
          type: string
          description: The channel region -- "USA", "Canada", "Europe", or "World"
        channel_model:
          maximum: 4
          minimum: 3
          type: integer
          description: The number of channel models to limit.
          format: int32
        channels:
          type: string
          description: The comma separated list of channels allowed channel switching
        enable_channel_auto_selection:
          type: boolean
          description: Whether to enable automatic channel switching during specified
            time interval.
        channel_selection_start_time:
          type: string
          description: The start time for channel switching in 24-hr time format of
            hh:mm
        channel_selection_end_time:
          type: string
          description: The end time for channel switching in 24-hr time format of
            hh:mm
        enable_avoid_switch_channel_if_clients_connected:
          type: boolean
          description: Whether to avoid channel switching if there are already max
            connected clients
        channel_selection_max_clients:
          type: integer
          description: The maximum number of connected clients from 0 up to 100 to
            avoid switching
          format: int32
        enable_switch_channel_if_exceed_threshold:
          type: boolean
          description: Whether to enable channel switching when RF interference exceeds
            the threshold
        rf_interference_threshold:
          type: integer
          description: The RF interference threshold from 10 up to 80 for channel
            switching.
          format: int32
        crc_error_threshold:
          type: integer
          description: The CRC error threshold from 10 up to 80 for channel switching.
          format: int32
      description: The payload of config for the radio profile
    XiqRadioMode:
      type: string
      description: The supported radio modes.
      enum:
        - B_G
        - G_N
        - A
        - A_N
        - AC
        - AX_2_4_GHZ
        - AX_5_GHZ
        - AX_6_GHZ
    XiqUpdateRadioProfileRequest:
      type: object
      properties:
        name:
          type: string
          description: The radio profile name
        description:
          type: string
          description: The radio profile description.
        transmission_power:
          maximum: 20
          minimum: 1
          type: integer
          description: The transmission power floor in the range of 1-20 dBm or null
            for Auto.
          format: int32
        max_transmit_power:
          maximum: 20
          minimum: 10
          type: integer
          description: The maximum transmit power in the range of 10-20 dBm.
          format: int32
        transmission_power_floor:
          maximum: 20
          minimum: 2
          type: integer
          description: The transmission power floor in the range of 2-20 dBm.
          format: int32
        transmission_power_max_drop:
          maximum: 18
          minimum: 0
          type: integer
          description: The transmission power max drop in the range of 0-18 dB.
          format: int32
        max_clients:
          maximum: 255
          minimum: 1
          type: integer
          description: The maximum number of clients in the range of 1-255.
          format: int32
        enable_client_transmission_power:
          type: boolean
          description: Whether or not client transmission power control (802.11h)
            is enabled.
        client_transmission_power:
          maximum: 20
          minimum: 1
          type: integer
          description: The client transmission power (in the range of 1-20 dBm) if
            it is enabled.
          format: int32
        radio_mode:
          "$ref": "#/components/schemas/XiqRadioMode"
        enable_ofdma:
          type: boolean
          description: Whether to enable Orthogonal Frequency Division Multiple Access
            (802.11ax) for multiple-user access by subdividing a channel.
    XiqSsidKeyType:
      type: string
      description: 'The key type, ASCII or HEX'
      example: ASCII
      enum:
        - ASCII
        - HEX
    XiqSsidWepAuthenticationMethod:
      type: string
      description: 'The authentication method type, OPEN or SHARED'
      enum:
        - OPEN
        - SHARED
    XiqSsidWepDefaultKey:
      type: string
      description: 'The default key, FIRST, SECOND,THIRD, OR FOURTH'
      example: FIRST
      enum:
        - FIRST
        - SECOND
        - THIRD
        - FOURTH
    XiqSsidWepEncryptionMethod:
      type: string
      description: 'The encryption method type, WEP104 or WEP40'
      example: WEP104
      enum:
        - WEP40
        - WEP104
    XiqSsidWepKeyManagement:
      type: string
      description: 'The key management type, WEP or WEP_8021X'
      example: WEP_8021X
      enum:
        - WEP
        - WEP_8021X
    XiqSetSsidModePskRequest:
      required:
        - encryption_method
        - key_management
        - key_type
        - key_value
      type: object
      properties:
        key_management:
          $ref: '#/components/schemas/XiqSsidPskKeyManagement'
        encryption_method:
          $ref: '#/components/schemas/XiqSsidPskEncryptionMethod'
        anti_logging_threshold:
          type: integer
          description: The anti logging threshold
          format: int32
        key_type:
          $ref: '#/components/schemas/XiqSsidKeyType'
        key_value:
          maxLength: 2147483647
          minLength: 8
          type: string
          description: 'The PSK key value, minimum 8 characters long'
        sae_group:
          $ref: '#/components/schemas/XiqSsidSaeGroup'
        transition_mode:
          type: boolean
          description: Indicates the transition mode if key management is WPA3
      description: The request for setting the SSID to be PSK mode.
    XiqSsidPskEncryptionMethod:
      type: string
      description: 'The encryption method: for WPA3_PSK please use CCMP, for WPA2_PSK or WPA_PSK, please use CCMP or TKIP, for AUTO_PSK, please use AUTO_TKIP_CCMP'
      enum:
        - CCMP
        - TKIP
        - AUTO_TKIP_CCMP
    XiqSsidPskKeyManagement:
      type: string
      description: 'The key management: WPA3_PSK, WPA2_PSK, WPA_PSK,AUTO_PSK'
      enum:
        - WPA_PSK
        - WPA2_PSK
        - WPA3_PSK
        - AUTO_PSK
    XiqSsidSaeGroup:
      type: string
      description: 'The SAE group: ALL, ECC, or FFC, must be selected when the key management is WPA3'
      enum:
        - ALL
        - ECC
        - FFC
    XiqSetSsidModePpskRequest:
      required:
        - enable_mac_bind
        - enable_max_clients_per_ppsk
        - encryption_method
        - key_management
        - user_group_ids
      type: object
      properties:
        key_management:
          $ref: '#/components/schemas/XiqSsidPpskKeyManagement'
        encryption_method:
          $ref: '#/components/schemas/XiqSsidPskEncryptionMethod'
        user_group_ids:
          type: array
          description: 'The user group IDs to be attached to the SSID, cannot be empty'
          items:
            type: integer
            description: 'The user group IDs to be attached to the SSID, cannot be empty'
            format: int64
        enable_max_clients_per_ppsk:
          type: boolean
          description: Flag for enabling the max clients per PPSK
        max_clients_per_ppsk:
          maximum: 15
          minimum: 0
          type: integer
          description: 'The max clients (0-15) per PPSK if enabled enable_max_clients_per_ppsk flag, 0 means unlimited'
          format: int32
        enable_mac_bind:
          type: boolean
          description: Flag for enabling mac binding or not. This setting is only supported with local PPSK.
        max_macs_per_ppsk:
          maximum: 5
          minimum: 1
          type: integer
          description: 'Set the max MAC binding numbers per private PSK, Min:1, Max:5'
          format: int32
        ppsk_server_id:
          type: integer
          description: The PPSK server device ID
          format: int64
      description: The request for setting the SSID to be PPSK mode.
    XiqSsidPpskKeyManagement:
      type: string
      description: 'The key management: WPA2_PSK, WPA_PSK,AUTO_PSK'
      enum:
        - WPA_PSK
        - WPA2_PSK
        - AUTO_PSK
    XiqSetSsidModeDot1xRequest:
      required:
        - enable_idm
        - encryption_method
        - key_management
      type: object
      properties:
        key_management:
          $ref: '#/components/schemas/XiqSsidDot1xKeyManagement'
        encryption_method:
          $ref: '#/components/schemas/XiqSsidDot1xEncryptionMethod'
        enable_idm:
          type: boolean
          description: Flag for using ExtremeCloud IQ Authentication Service or not
        transition_mode:
          type: boolean
          description: Flag for enabling transition mode if using WPA3 as the key management type
        radius_server_group_id:
          type: integer
          description: The RADIUS server group ID if not using ExtremeCloud IQ Authentication Service
          format: int64
        user_group_ids:
          type: array
          description: The user group IDs if using ExtremeCloud IQ Authentication Service
          items:
            type: integer
            description: The user group IDs if using ExtremeCloud IQ Authentication Service
            format: int64
      description: The request for setting the SSID to be 802.1x mode.
    XiqSsidDot1xEncryptionMethod:
      type: string
      description: 'The encryption method: for WPA_8021X, WPA2_8021X, please use CCMP or TKIP, for AUTO_8021X, please use AUTO_TKIP_CCMP, for WPA3_8021X, please use AES192'
      example: CCMP
      enum:
        - CCMP
        - TKIP
        - AUTO_TKIP_CCMP
        - AES192
    XiqSsidDot1xKeyManagement:
      type: string
      description: 'The key management: WPA_8021X, WPA2_8021X, WPA3_8021X or AUTO_8021X'
      example: WPA3_8021X
      enum:
        - WPA_8021X
        - WPA2_8021X
        - WPA3_8021X
        - AUTO_8021X
    XiqClassification:
      required:
        - classification_id
        - classification_type
        - create_time
        - id
        - match
        - update_time
        - value
      type: object
      properties:
        id:
          type: integer
          description: The unique identifier
          format: int64
        create_time:
          type: string
          description: The create time
          format: date-time
        update_time:
          type: string
          description: The last update time
          format: date-time
        classification_type:
          $ref: '#/components/schemas/XiqClassificationType'
        match:
          type: boolean
          description: Contains or not contains)
        classification_id:
          type: integer
          description: 'The ID of location, cloud config group, IP address, IP subnet or IP range.'
          format: int64
        value:
          type: string
          description: The value of classification
      description: The payload of common object - classification
    XiqClassificationRule:
      required:
        - create_time
        - id
        - name
        - update_time
      type: object
      properties:
        id:
          type: integer
          description: The unique identifier
          format: int64
        create_time:
          type: string
          description: The create time
          format: date-time
        update_time:
          type: string
          description: The last update time
          format: date-time
        name:
          type: string
          description: The classification assignment name
        description:
          type: string
          description: The classification assignment description
        classifications:
          type: array
          description: The details of rule assignments
          items:
            $ref: '#/components/schemas/XiqClassification'
      description: The payload of common object - classification assignment
    XiqClassificationType:
      type: string
      description: Classification type
      enum:
        - LOCATION
        - CLOUD_CONFIG_GROUP
        - IP_ADDRESS
        - IP_SUBNET
        - IP_RANGE
    XiqUpdateClassificationRequest:
      required:
        - classification_type
        - classification_type_id
        - match
      type: object
      properties:
        classification_type:
          type: string
          description: Classification type
          enum:
            - CLASSIFICATION_TYPE_UNSPECIFIED
            - CLASSIFICATION_TYPE_LOCATION
            - CLASSIFICATION_TYPE_CLOUD_CONFIG_GROUP
            - CLASSIFICATION_TYPE_IP_ADDRESS
            - CLASSIFICATION_TYPE_IP_SUBNET
            - CLASSIFICATION_TYPE_IP_RANGE
            - UNRECOGNIZED
        match:
          type: boolean
          description: Contains or not contains)
        classification_type_id:
          type: integer
          description: 'The ID of location, cloud config group, IP address, IP subnet or IP range.'
          format: int64
      description: The details of rule Rules
    XiqUpdateClassificationRuleRequest:
      required:
        - classifications
        - name
      type: object
      properties:
        name:
          type: string
          description: The name of classification assignment
        description:
          type: string
          description: The description of classification assignment
        classifications:
          type: array
          description: The details of rule Rules
          items:
            $ref: '#/components/schemas/XiqUpdateClassificationRequest'
    PagedXiqSsid:
      required:
        - count
        - page
        - total_count
        - total_pages
      type: object
      properties:
        page:
          type: integer
          description: The current page number
          format: int32
        count:
          type: integer
          description: The element count of the current page
          format: int32
        total_pages:
          type: integer
          description: The total page number based on request page size
          format: int32
        total_count:
          type: integer
          description: The total element count
          format: int64
        data:
          type: array
          description: The data in the current page
          items:
            $ref: '#/components/schemas/XiqSsid'
    XiqSsid:
      required:
        - broadcast_name
        - create_time
        - id
        - name
        - predefined
        - update_time
      type: object
      properties:
        id:
          type: integer
          description: The unique identifier
          format: int64
        create_time:
          type: string
          description: The create time
          format: date-time
        update_time:
          type: string
          description: The last update time
          format: date-time
        org_id:
          type: integer
          description: 'The organization identifier, valid when enabling HIQ feature'
          format: int64
        name:
          type: string
          description: The SSID profile name
        broadcast_name:
          type: string
          description: The SSID broadcast name
        description:
          type: string
          description: The SSID description
        predefined:
          type: boolean
          description: Whether it is predefined
      description: The SSID
    PagedXiqUserProfile:
      required:
        - count
        - page
        - total_count
        - total_pages
      type: object
      properties:
        page:
          type: integer
          description: The current page number
          format: int32
        count:
          type: integer
          description: The element count of the current page
          format: int32
        total_pages:
          type: integer
          description: The total page number based on request page size
          format: int32
        total_count:
          type: integer
          description: The total element count
          format: int64
        data:
          type: array
          description: The data in the current page
          items:
            $ref: '#/components/schemas/XiqUserProfile'
    XiqUserProfile:
      required:
        - create_time
        - id
        - name
        - update_time
      type: object
      properties:
        id:
          type: integer
          description: The unique identifier
          format: int64
        create_time:
          type: string
          description: The create time
          format: date-time
        update_time:
          type: string
          description: The last update time
          format: date-time
        name:
          type: string
          description: The user profile name
        vlan_profile:
          $ref: 'configuration-basic.yaml#/components/schemas/XiqVlanProfile'
      description: The payload of User Profile
    XiqCreateUserProfileRequest:
      required:
        - name
        - vlan_profile_id
      type: object
      properties:
        name:
          type: string
          description: The user profile name
        vlan_profile_id:
          type: integer
          description: The VLAN profile id
          format: int64
    XiqUpdateUserProfileRequest:
      type: object
      properties:
        name:
          type: string
          description: The user profile name
        vlan_profile_id:
          type: integer
          description: The VLAN profile id
          format: int64