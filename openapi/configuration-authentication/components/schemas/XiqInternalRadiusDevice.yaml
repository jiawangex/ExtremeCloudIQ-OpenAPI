allOf:
  - $ref: ../../../common/components/schemas/XiqViqEntity.yaml
  - type: object
    description: The configuration of internal RADIUS device object
    required:
      - radius_server_id
      - radius_server_name
      - host_name
    properties:
      radius_server_id:
        type: integer
        description: The RADIUS server id
        format: int64
      radius_server_name:
        type: string
        description: The RADIUS server name
      host_name:
        type: string
        description: The RADIUS server device hostname
