type: object
description: data sequence
required:
  - dfs_channel_stats
  - min_channel
  - max_channel
properties:
  dfs_channel_stats:
    type: array
    description: The data in the current page
    items:
      $ref: ./XiqChannelStats.yaml
  min_channel:
    type: integer
  max_channel:
    type: integer