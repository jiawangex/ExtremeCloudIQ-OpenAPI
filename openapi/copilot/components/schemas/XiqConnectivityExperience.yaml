type: object
description: Connectivity experience entity
required:
  - id
  - name
  - qualityIndex
  - trendIndicator
  - connectScore
  - graph
properties:
  id:
    type: integer
    format: int64
  name:
    type: string
  qualityIndex:
    type: integer
    minimum: 1
    maximum: 10
  trendIndicator:
    type: string
  connectScore:
    type: integer
    minimum: 1
    maximum: 10
  graph:
    type: array
    description: The data in the current page
    items:
      type: object
      description: Connectivity experience sequence value
      required:
        - timestamp
        - data
      properties:
        timestamp:
          type: integer
          format: int64
        data:
          type: integer
          minimum: 1
          maximum: 10