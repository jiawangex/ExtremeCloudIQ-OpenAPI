type: object
required:
  - name
properties:
  name:
    type: string
    description: The organization name, must be unique in VIQ
  color:
    type: string
    description: The tagged color for the organization
